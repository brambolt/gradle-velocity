
task runSimple(type: Exec) {
  workingDir = new File(project.projectDir, 'simple')
  commandLine(['../../gradlew', 'velocity', '--no-daemon', "-PbramboltVersion=${bramboltVersion}"])
}

task runAll {
  dependsOn(runSimple)
}
